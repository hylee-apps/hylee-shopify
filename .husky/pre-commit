#!/usr/bin/env sh

echo "🔍 Running pre-commit checks..."

# Always check formatting
echo "💅 Checking code formatting..."
pnpm format:check || exit 1

# TypeScript check is skipped locally — tsc OOMs at 2GB on this machine.
# pnpm build catches type errors at the Vite layer; full tsc runs in CI.

echo "✅ Pre-commit checks passed!"
