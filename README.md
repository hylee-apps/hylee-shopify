
  # Dropshipping Platform Wireframe (Copy)

  This is a code bundle for Dropshipping Platform Wireframe (Copy). The original project is available at https://www.figma.com/design/0YCwzIhUFSmXUtg2cKFEBM/Dropshipping-Platform-Wireframe--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  